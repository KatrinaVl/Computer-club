Компиляция файла происходит с помощью gcc/g++.

В терминал вводится команда: g++ main.cpp -o result

С ее помощью код будет скомпилирован в файл result. Затем его необходимо запустить вместе с именем тестового файла:
./<name_compl_file> test/<name_folder>/<name_file>.txt
./result test/simple.txt

Тесты располагаются в папке tests. Внутри они разделены по смыслу:

- check_events - проверка входных данных для событий.
- check_num_table - проверка входных данных для количества столов(цена за час не проверяется,
так как формат совпадает с количеством столов).
- check_time - проверка входных данных для времени.
- events - тесты различных ситуаций.
